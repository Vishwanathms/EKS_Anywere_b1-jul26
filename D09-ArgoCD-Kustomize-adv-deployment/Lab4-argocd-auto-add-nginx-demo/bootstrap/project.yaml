apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: gitops-training
  namespace: argocd

spec:
  description: GitOps Training Project

  sourceRepos:
    - '*'

  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc

  clusterResourceWhitelist:
    - group: '*'
      kind: '*'

  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'

  orphanedResources:
    warn: true