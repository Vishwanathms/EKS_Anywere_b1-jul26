Yes. I would revise the lab so that students build the **entire CI/CD flow from scratch**, including the Linux CI machine.

The architecture for this lab should be:

```text
                           GitHub
                              |
                    Developer git push
                              |
                              v
                    +----------------+
                    |   Harness CI   |
                    +----------------+
                              |
                    Linux CI Machine
                    +----------------+
                    | Docker Delegate|
                    | Docker Runner   |
                    +----------------+
                              |
              +---------------+---------------+
              |                               |
              v                               v
        Build Python App               Build Docker Image
                                              |
                                              v
                                         Docker Hub
                                              |
                                              v
                                      Update K8s Manifest
                                              |
                                              v
                                           GitHub
                                              |
                                      GitHub Webhook
                                              |
                                              v
                                      +---------------+
                                      |   Harness CD  |
                                      +---------------+
                                              |
                                         Delegate
                                              |
                                              v
                                        EKS Anywhere
                                              |
                                              v
                                     Rolling Deployment
```

Harness currently supports a **self-managed Linux local runner** using a Harness Docker Delegate plus Harness Docker Runner. The CI work runs on that Linux host; the Delegate communicates with Harness. ([Harness][1])

Below is the updated complete lab.

# Module 9 — Harness End-to-End CI/CD Lab

# GitHub → Harness CI → Docker Hub → Git Manifest Update → Harness CD → EKS Anywhere

---

# LAB OBJECTIVE

Build a complete CI/CD pipeline for the existing Python + Redis application.

By the end of this lab:

```text
Developer
   |
   | git push
   v
GitHub
   |
   v
Harness CI
   |
   v
Linux CI Machine
   |
   +--> Clone
   +--> Build
   +--> Test
   +--> Docker Build
   +--> Docker Push
   |
   v
Docker Hub
   |
   v
Update Kubernetes Manifest
   |
   v
GitHub
   |
   v
Harness CD
   |
   v
EKS Anywhere
   |
   v
Rolling Deployment
```

The final demonstration should be:

> Change `app.py` → `git push` → Harness CI automatically builds → Docker image is pushed → Kubernetes manifest is updated → GitHub receives the change → Harness CD automatically deploys → new application version runs on EKS Anywhere.

---

# PART 1 — LAB ARCHITECTURE

## 1. Components Required

You will use:

### Source Code

```text
GitHub
```

### CI Platform

```text
Harness SaaS
```

### CI Worker

A dedicated:

```text
Ubuntu Linux VM
```

running:

```text
Harness Docker Delegate
Harness Docker Runner
Docker
```

Harness's local runner architecture requires a Delegate and Runner relationship; the current documentation specifies one local runner per Delegate/host. ([Harness][1])

### Container Registry

```text
Docker Hub
```

### CD Target

```text
EKS Anywhere
```

### Kubernetes Application

```text
Python + Redis
```

---

# PART 2 — CREATE DOCKER HUB REPOSITORY

## 2. Create Docker Hub Account / Repository

Open:

```text
https://hub.docker.com
```

Login.

Create a repository:

```text
python-redis-app
```

Example:

```text
Docker Hub Username:
student123

Repository:
python-redis-app
```

The final image will be:

```text
docker.io/student123/python-redis-app
```

---

# 3. Create Docker Hub Access Token

Do not use the Docker Hub password in Harness.

In Docker Hub:

```text
Account
 → Account Settings
 → Personal Access Tokens
 → Generate New Token
```

Example:

```text
Token name:
harness-training

Permission:
Read & Write
```

Copy the token.

You will use:

```text
Username
+
Access Token
```

when creating the Harness Docker connector.

---

# PART 3 — PREPARE THE LINUX CI MACHINE

## 4. Linux VM

For the training lab, use one dedicated VM per training environment or one shared CI VM, depending on your lab design.

Recommended starting point:

```text
OS: Ubuntu 24.04
CPU: 4 vCPU
RAM: 8 GB
Disk: 50 GB+
Network: Internet access
```

The machine needs outbound connectivity to Harness and Docker Hub/GitHub.

You do not need to install a Harness Delegate on this machine for the Kubernetes CD portion if your existing EKS Anywhere Delegate already handles CD.

This Linux machine is specifically for:

```text
Harness CI
```

---

# 5. Update Ubuntu

SSH to the Linux VM:

```bash
ssh student@<LINUX-CI-IP>
```

Update:

```bash
sudo apt update
sudo apt upgrade -y
```

Install basic tools:

```bash
sudo apt install -y \
  curl \
  wget \
  git \
  ca-certificates \
  unzip \
  jq
```

---

# 6. Install Docker

Install Docker:

```bash
curl -fsSL https://get.docker.com | sudo sh
```

Enable Docker:

```bash
sudo systemctl enable docker
sudo systemctl start docker
```

Verify:

```bash
docker version
```

Verify the service:

```bash
sudo systemctl status docker
```

Expected:

```text
Active: active (running)
```

---

# 7. Allow the Training User to Run Docker

Run:

```bash
sudo usermod -aG docker $USER
```

Log out:

```bash
exit
```

SSH back into the VM.

Verify:

```bash
docker ps
```

Expected:

```text
CONTAINER ID   IMAGE   COMMAND   ...
```

The important point:

```text
docker ps
```

must work without:

```text
sudo
```

---

# PART 4 — INSTALL HARNESS DOCKER DELEGATE

## 8. Create the CI Delegate in Harness

Open:

```text
Harness
 → Account Settings
 → Account Resources
 → Delegates
```

Create a new Delegate.

Choose:

```text
Docker
```

Give it a meaningful name:

```text
ci-linux-delegate
```

Choose the Linux AMD64 configuration appropriate for the VM.

Harness currently documents Docker-based Delegate installation and Delegate tags such as `linux-amd64` for Linux AMD64 environments. ([Harness][2])

---

# 9. Generate the Delegate Installation Command

Harness will show an installation command similar to:

```bash
docker run ...
```

Copy the command from the Harness UI.

Do not type a token manually.

The exact command is account-specific.

Run the command on:

```text
Linux CI VM
```

---

# 10. Add the Linux Tag

The CI machine is Linux AMD64.

Ensure the Delegate has a tag such as:

```text
linux-amd64
```

or the exact tag configured by your Harness-generated command.

Harness's current local-runner setup specifically recommends matching the Delegate tag to the machine OS/architecture. ([Harness][1])

---

# 11. Verify the Delegate

In Harness:

```text
Account Settings
 → Delegates
```

Expected:

```text
ci-linux-delegate
Status: Connected
```

The Delegate is now connected to Harness.

But:

> **The Delegate alone does not execute the CI build.**

We still need the:

```text
Harness Docker Runner
```

---

# PART 5 — INSTALL HARNESS DOCKER RUNNER

## 12. Download the Harness Docker Runner

Harness provides the Runner executable for the supported Linux architecture.

Use the current download link from:

```text
Harness
 → CI
 → Build Infrastructure
 → Local Runner
```

The current Harness documentation instructs you to download the Docker Runner corresponding to your operating system and architecture. ([Harness][1])

For Ubuntu 24 AMD64, use the Linux AMD64 Runner.

After downloading, verify:

```bash
ls -lh
```

---

# 13. Make the Runner Executable

Example:

```bash
chmod +x harness-docker-runner-linux-amd64
```

Verify:

```bash
ls -l harness-docker-runner-linux-amd64
```

You should see executable permissions.

---

# 14. Start the Harness Docker Runner

Run:

```bash
sudo ./harness-docker-runner-linux-amd64 server
```

The exact runner filename may differ based on the current Harness download.

The current Harness documentation shows starting the Runner with the `server` argument. ([Harness][1])

The Runner should now listen on its local runner port.

The Delegate must be configured to communicate with the Runner.

---

# 15. Connect Delegate to Runner

The local runner architecture is:

```text
Harness SaaS
    |
    v
Docker Delegate
    |
    v
Docker Runner
    |
    v
CI Build
```

The Delegate and Runner are designed as a pair for the local runner build infrastructure. ([Harness][1])

The current Harness installation instructions provide the appropriate Runner connection settings as part of the generated Delegate configuration.

---

# 16. Verify CI Machine

On the Linux VM:

```bash
docker ps
```

You should see the Harness Delegate container.

Also verify:

```bash
ps -ef | grep harness
```

You should see the Runner process.

In Harness:

```text
Account
 → Delegates
```

Verify the Delegate:

```text
Connected
```

Then verify the Runner/local build infrastructure is available from the CI pipeline configuration.

---

