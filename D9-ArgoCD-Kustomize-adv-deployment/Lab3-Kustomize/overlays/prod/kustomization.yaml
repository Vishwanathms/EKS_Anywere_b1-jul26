apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx-kust-prod

resources:
  - ../../base

patchesStrategicMerge:
  - replica-patch.yaml
  - deployment-patch.yaml
  - ingress-patch.yaml

commonLabels:
  environment: prod