apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-demo

spec:

  template:

    metadata:

      labels:

        environment: prod

    spec:

      containers:

      - name: nginx

        image: nginx:1.28

        env:

        - name: ENVIRONMENT
          value: PROD

        - name: COLOR
          value: green